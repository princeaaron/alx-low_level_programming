#include "main.h"
#include <stdio.h>

/**
 * mul - multiplication of two integers
 * @a: first number
 * @b: second number
 * Return: multiplication of a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
